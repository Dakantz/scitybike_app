overwrite: true
schema: "http://localhost:8080"
documents: "./queries/**/*"
generates:
  generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
